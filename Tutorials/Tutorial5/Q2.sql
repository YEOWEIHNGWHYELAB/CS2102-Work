DROP TABLE IF EXISTS R , S CASCADE;

CREATE TABLE R (
	a INTEGER PRIMARY KEY,
	b INTEGER,
	c INTEGER
);

-- TABLE is unused in this question.
CREATE TABLE S (
	x INTEGER PRIMARY KEY,
	y INTEGER REFERENCES R(a)
);

SELECT COUNT (C)
FROM R
WHERE a = 10;

SELECT COUNT (C)
FROM R
WHERE a = 10
GROUP BY a;